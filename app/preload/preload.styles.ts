import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        // backgroundColor: '#E08500',
        backgroundColor: '#cc8330',
        justifyContent: 'center',
        alignItems: 'center',
    },
    icon: {
        width: 120,
        height: 120,
    },
});

export default styles;
